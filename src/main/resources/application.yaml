# App configuration
server:
  port: 8189
  servlet:
    context-path: /doe

# Spring
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
#      ddl-auto: validate
      ddl-auto: create
  flyway:
    enabled: true
    baseline-on-migrate: true
    schemas: doe
